extends Node2D

# thinking of using this as the 'base' for all battle scenes (tutorial, regular encounters)
# these just include the grid? + script for battle logic
# each new 'battle scene' will have 'base battle' as root (or first child), 
# a HUD scene child, background/floor scene child?, player + enemy character nodes


# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
